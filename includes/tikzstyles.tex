\tikzset{
  %Define standard arrow tip
  >=stealth',
  %Define style for boxes
  punkt/.style={
    rectangle,
    rounded corners,
    draw=black, very thick,
    text width=4em, 
    minimum height=1em,
    text centered},
    % Define arrow style
    pil/.style={
      ->,
      thin,
      shorten <=2pt,
      shorten >=2pt,
    },
    dummyheight/.style={
        punkt,fill=none,
        draw=black, dashed, thin
    }
}